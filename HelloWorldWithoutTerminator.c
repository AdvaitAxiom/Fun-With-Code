#include<stdio.h>

int main(){
    if(printf("Hello World!")){}

    //Here printf is in the control statement so it have to return
    //printf returns 12 after printing it(No of characters)
    //if get 12(!=0)( = true) so if body executes
}